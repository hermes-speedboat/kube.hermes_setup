# Profile defaults apply only when the operator did not set the corresponding
# HERMES_* variable in hermes.env or the process environment.
HERMES_PROFILE_DEFAULT_SSH_SETUP=true
HERMES_PROFILE_DEFAULT_ANSIBLE_SETUP=true
HERMES_PROFILE_DEFAULT_ADDON_REQUIREMENTS=requirements.txt
